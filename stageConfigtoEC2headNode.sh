#!/bin/bash

#stage data from local directory to the head node

#Stack config details
#aws credentials
#job config
#nextflow config with aws credentials
	#move them to ~/.nextflow and do same with permissions?


